{% extends 'base.html.twig' %}

{% block title %}Snowtricks - Reinitialiser password{% endblock %}

{% block body %}

<div class="container">
    <h1>Réinitialiser votre mot de passe :</h1>
    <hr>
    <p class="mb-5">{{ username }}, vous pouvez choisir un autre mot de passe :</p>
    {{ form_start(resetPasswordForm) }}
    {{ form_row(resetPasswordForm.password, {'label' : 'Nouveau mot de passe :'}) }}
    {{ form_row(resetPasswordForm.confirmPassword, {'label' : 'Confirmation du mot de passe :'}) }}
    <button type="submit" class="btn btn-outline-primary">Reinitialiser</button>
    {{ form_end(resetPasswordForm) }}
</div>

{% endblock %}